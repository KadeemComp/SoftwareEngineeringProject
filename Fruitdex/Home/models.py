from django.db import models

# Create your models here.

#class Fruits(models.Model):
    
    #image = models.ImageField(upload_to = )#FireBaseStorage URL)
    #last_name = models.CharField(max_length=64)
